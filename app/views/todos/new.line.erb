<%= raw(Kamiflex.build(self) do
  bubble do
    body do
      text "POST /todos\n#{ {todo:{name: "顯示名稱"}}.to_json }", wrap: true
    end
    quick_reply do
      message_action_item "試試看", text: "POST /todos\n#{ {todo:{name: "顯示名稱"}}.to_json }"
    end
  end
end )%>